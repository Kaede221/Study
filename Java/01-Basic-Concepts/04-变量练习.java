public class Test {
    public static void main(String[] args) {
        /* 假设
         * 一个站数 每站可以上下人
         * 求最后的人数
         * */
        int count = 10;
        //可以这样子, 等效于count = count + 1
        count += 1;
        count *= 2;
        count -= 9;
        System.out.println(count);
        /* 个人说说
         * 其实练习, 没啥练习的
         * 就是基本的变量操作而已
         * */
    }
}