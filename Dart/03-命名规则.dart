// 和其他的语言基本上是一样的
// 只能输数字 字母 下划线组成的
// 不能数字开头
void main(List<String> args) {
  // var str1 = ""; 合法定义
  // var 2str = ""; 非法定义

  //另外 不能是关键字保留字
  // var if = 10;

  //同时 变量名区分大小写
  var num = 10;
  var NUM = 20;
  print(num + NUM);

  // 另外, 变量要见名知意
  // 不要使用类似于abc之类的变量名, 否则会拖慢开发效率
}
