#include<iostream>
using namespace std;

//函数，其实就是把许多常用的代码，封装成一个代码
//这样子可以更加简单的进行代码复用，减少重复
//其实main就是一个函数 前面的类型就是返回值类型，main就是函数名，括号里面可以有参数，就是传入的数据

//比如我可以定义一个HelloWorld的函数
//需要它不返回内容
void helloWorld() {
	//因为没有返回值，所以可以直接return
	return;
}

int main() {

	return 0;
}